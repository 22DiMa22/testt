const message = "Hello Java Script";

const letter = message[4];
console.log(letter);

// як подивитися довжину рядка:
const mesLengs = message.length;
console.log(mesLengs);

// знайдемо index истаннього елемента в рядку:
const lastIndex = message.length - 1;
console.log(lastIndex);

// знайдемо останню букву по індексу:
const lastLeter = message[lastIndex];
console.log(lastLeter);